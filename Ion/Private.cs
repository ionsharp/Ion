using System;

namespace Ion;

/// <summary>
/// A <see langword="class"/> or <see langword="struct"/> that is self-maintained (no outside references).
/// </summary>
public sealed class PrivateAttribute() : Attribute();